import React, { Component } from 'react';
import { Grid, Panel } from 'react-bootstrap';

export default class Thanks extends Component {
    render() {
      return (
        <div class="container">
          <Panel>
              TODO: Make this into a "View Order" view, with order number, plan, and price.

              Thanks for your order! You first meals will be shipped on "ship day".
          </Panel>
        </div>
      );
    }
}